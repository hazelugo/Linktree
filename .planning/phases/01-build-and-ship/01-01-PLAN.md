---
phase: 01-build-and-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - LINK-01
  - LINK-02
  - LINK-03
  - LINK-04

must_haves:
  truths:
    - "Page has a descriptive <title> visible in the browser tab"
    - "Avatar image, name, and tagline are present in the HTML and centered"
    - "Four link buttons exist: GitHub, Amazon Wishlist, CashApp, PayPal"
    - "Each button links to its correct destination URL and opens in a new tab"
  artifacts:
    - path: "index.html"
      provides: "Complete HTML document structure"
      contains: "<title>Hector"
    - path: "index.html"
      provides: "Profile header section"
      contains: "class=\"avatar\""
    - path: "index.html"
      provides: "Four link buttons"
      contains: "github.com"
  key_links:
    - from: "index.html anchor[GitHub]"
      to: "https://github.com/HectorCastelli"
      via: "href attribute with target=_blank"
      pattern: "github\\.com.*target"
    - from: "index.html link[stylesheet]"
      to: "style.css"
      via: "<link rel=\"stylesheet\">"
      pattern: "rel=\"stylesheet\""
---

<objective>
Build the complete HTML skeleton for the personal linktree page: document boilerplate, profile header (avatar, name, tagline), four link/donation buttons, and a linked stylesheet reference.

Purpose: Establish all markup required for Plan 02 to apply styling against. Every class name and element structure defined here is the target for CSS rules.
Output: index.html — a fully valid HTML5 document with all content present, ready for styling.
</objective>

<execution_context>
@C:/Users/Hector/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Hector/Documents/Linktree/.planning/PROJECT.md
@C:/Users/Hector/Documents/Linktree/.planning/ROADMAP.md
@C:/Users/Hector/Documents/Linktree/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create index.html with document skeleton, profile header, and link buttons</name>
  <files>index.html</files>
  <read_first>
    - C:/Users/Hector/Documents/Linktree/.planning/REQUIREMENTS.md (link URLs and label text required for each button)
    - C:/Users/Hector/Documents/Linktree/.planning/PROJECT.md (profile details and constraints)
  </read_first>
  <action>
Create `index.html` at the project root with the following exact structure. Do not use a framework or build tool — pure HTML5.

**Document skeleton:**
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Hector Castelli — Links</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  ...
</body>
</html>
```

**Profile header** inside `<body>`, wrapped in `<main class="container">`:
```html
<header class="profile">
  <img
    class="avatar"
    src="avatar.jpg"
    alt="Hector Castelli"
    width="96"
    height="96"
  />
  <h1 class="name">Hector Castelli</h1>
  <p class="tagline">Software engineer. Builder of things.</p>
</header>
```

**Link buttons** directly after the `<header>`, inside the same `<main class="container">`, inside a `<nav class="links">`:
```html
<nav class="links">
  <a
    class="link-button"
    href="https://github.com/HectorCastelli"
    target="_blank"
    rel="noopener noreferrer"
  >GitHub</a>
  <a
    class="link-button"
    href="https://www.amazon.com/hz/wishlist/ls/YOUR_LIST_ID"
    target="_blank"
    rel="noopener noreferrer"
  >Amazon Wishlist</a>
  <a
    class="link-button"
    href="https://cash.app/$HectorCastelli"
    target="_blank"
    rel="noopener noreferrer"
  >CashApp</a>
  <a
    class="link-button"
    href="https://www.paypal.com/donate/?hosted_button_id=YOUR_BUTTON_ID"
    target="_blank"
    rel="noopener noreferrer"
  >PayPal</a>
</nav>
```

Notes:
- The Amazon wishlist URL placeholder `YOUR_LIST_ID` and PayPal placeholder `YOUR_BUTTON_ID` should remain as-is — Hector will replace them with real values.
- `avatar.jpg` is a placeholder filename. Hector will provide the actual image.
- The `style.css` link is intentionally pointing to a file that Plan 02 will create. The page will render unstyled until Plan 02 is complete — that is expected.
- Use `rel="noopener noreferrer"` on every `target="_blank"` link (security best practice).
- No inline styles anywhere — all styling is deferred to `style.css`.
- No JavaScript — this page is pure HTML/CSS.
  </action>
  <verify>
    <automated>
      grep -c "class=\"link-button\"" C:/Users/Hector/Documents/Linktree/index.html
      # Must output 4 (one per button)

      grep "github.com/HectorCastelli" C:/Users/Hector/Documents/Linktree/index.html
      # Must find the GitHub href

      grep "target=\"_blank\"" C:/Users/Hector/Documents/Linktree/index.html | wc -l
      # Must output 4

      grep "rel=\"stylesheet\"" C:/Users/Hector/Documents/Linktree/index.html
      # Must find stylesheet link to style.css

      grep "<title>Hector" C:/Users/Hector/Documents/Linktree/index.html
      # Must find descriptive title

      grep "class=\"avatar\"" C:/Users/Hector/Documents/Linktree/index.html
      # Must find avatar img element
    </automated>
  </verify>
  <acceptance_criteria>
    - index.html exists at project root
    - index.html contains `&lt;title&gt;Hector Castelli` (descriptive browser tab title)
    - index.html contains `class="avatar"` (avatar image element)
    - index.html contains `class="name"` (h1 name element)
    - index.html contains `class="tagline"` (p tagline element)
    - index.html contains exactly 4 elements with `class="link-button"`
    - index.html contains `href="https://github.com/HectorCastelli"` with `target="_blank"`
    - index.html contains `cash.app` href with `target="_blank"`
    - index.html contains `paypal.com` href with `target="_blank"`
    - index.html contains `amazon.com` href with `target="_blank"`
    - index.html contains `rel="noopener noreferrer"` on all external links (4 occurrences)
    - index.html contains `&lt;link rel="stylesheet" href="style.css"`
    - No inline `style=` attributes anywhere in the file
    - No `&lt;script&gt;` tags in the file
  </acceptance_criteria>
  <done>index.html is a valid HTML5 document with profile header, four link buttons pointing to correct destinations, a descriptive title, and a stylesheet reference — ready for Plan 02 to apply CSS.</done>
</task>

</tasks>

<verification>
Open index.html in a browser (file:// or local server). Without style.css present:
- Page renders with plain browser default styling
- Four button links are visible and clickable
- Title in browser tab reads "Hector Castelli — Links" (or similar descriptive text)
- Clicking each link opens the correct URL in a new tab
</verification>

<success_criteria>
- index.html exists and is valid HTML5
- Browser tab title is descriptive (contains "Hector" — not "index" or blank)
- Avatar, name, and tagline are present in the markup
- All four buttons (GitHub, Amazon Wishlist, CashApp, PayPal) have correct hrefs and open in new tab
- rel="noopener noreferrer" present on all four external links
- No inline styles, no JavaScript
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-and-ship/01-01-SUMMARY.md` using the summary template at `@C:/Users/Hector/.claude/get-shit-done/templates/summary.md`.
</output>
